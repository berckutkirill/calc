<div class="form-group">
    <div class="row">
        <div class="col-6">
            <label for="{{prefix}}{{code}}">{{title}}</label>
        </div>
        <div class="col-6">
            <input type="checkbox" id="{{prefix}}{{code}}" name="{{code}}" class="form-control" value="true" {{#checked}}checked{{/checked}} />
        </div>
    </div>
</div>

